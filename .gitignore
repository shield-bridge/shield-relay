# dependencies
node_modules/

# build output
dist/
*.tsbuildinfo

# runtime data + secrets (NEVER commit)
data/
*.db
*.db-*
secrets/
pool.json
*.pem
*.key
.env
.env.*
!.env.example

# sapling proving params (baked into image at build, not committed)
params/

# logs / os
*.log
.DS_Store
coverage/

# Local one-time operator tools (not part of the published relay)
scripts/register-serverless-relay.mjs
scripts/keys-to-addresses.mjs
